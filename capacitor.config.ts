import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.compasscrm.app',
  appName: 'Compass CRM',
  webDir: 'dist/AlliantCRM',
  bundledWebRuntime: false
};

export default config;
